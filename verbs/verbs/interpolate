A three-state model is therefore used to interpolate the thermal denaturation data .
Regression analysis was used to interpolate values for all other possible health states .
The coefficients of these fits are used to interpolate the system matrix .
They also may be used to interpolate to single years of age .
We also propose approximate isotherms that interpolate between the exact results .
This paper proposes a new optical flow based approach to interpolate between sparse data .
The proposed method is able to interpolate the missing lines without introducing annoying articles .
In some cases it has been necessary to interpolate data due to missing data .
Contemporary GIS and model-based geostatistics can be used to interpolate malaria risk in Bangladesh .
We view the lines to interpolate as gaps that we need to inpaint .
The correlators interpolate between three scenarios .
We interpolate between two known approximations by matching fourth moments .
When does human vision interpolate a contour between a given pair of luminance-defined edges ?
The resulting patching meshes interpolate the shape and density of the surrounding mesh .
The resulting patching meshes interpolate the shape and density of the surrounding mesh .
We interpolate this result to arbitrary gamma > or =2 .

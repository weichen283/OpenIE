obj	A three-state model is therefore used to interpolate the thermal denaturation data .	obj@data
obj	Regression analysis was used to interpolate values for all other possible health states .	obj@values
obj	The coefficients of these fits are used to interpolate the system matrix .	obj@matrix

prep	They also may be used to interpolate to single years of age .	prep_to@years
prep	We also propose approximate isotherms that interpolate between the exact results .	prep_between@results
prep	This paper proposes a new optical flow based approach to interpolate between sparse data .	prep_between@data

obj#prep	The proposed method is able to interpolate the missing lines without introducing annoying articles .	obj@lines prepc_without@introducing
obj#prep	In some cases it has been necessary to interpolate data due to missing data .	obj@data prep_due_to@data
obj#prep	Contemporary GIS and model-based geostatistics can be used to interpolate malaria risk in Bangladesh .	obj@risk prep_in@Bangladesh

subj#prep	We view the lines to interpolate as gaps that we need to inpaint .	subj@lines prep_as@gaps
subj#prep	The correlators interpolate between three scenarios .	subj@correlators prep_between@scenarios
subj#prep	We interpolate between two known approximations by matching fourth moments .	subj@We prep_between@approximations prepc_by@matching

subj#obj	When does human vision interpolate a contour between a given pair of luminance-defined edges ?	subj@vision obj@contour
subj#obj	The resulting patching meshes interpolate the shape and density of the surrounding mesh .	subj@meshes obj@shape
subj#obj	The resulting patching meshes interpolate the shape and density of the surrounding mesh .	subj@meshes obj@shape

subj#obj#prep	We interpolate this result to arbitrary gamma > or =2 .	subj@We obj@result prep_to@>

